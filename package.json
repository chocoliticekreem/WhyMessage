{
  "name": "whymessage",
  "version": "0.1.0",
  "description": "Text a friend's name to see your relationship profile. Text a need to find the right person.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx --env-file=.env src/index.ts",
    "seed": "tsx scripts/seed-cache.ts"
  },
  "dependencies": {
    "@photon-ai/imessage-kit": "latest",
    "better-sqlite3": "^11.0.0",
    "dotenv": "^16.4.0",
    "openai": "^6.34.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
